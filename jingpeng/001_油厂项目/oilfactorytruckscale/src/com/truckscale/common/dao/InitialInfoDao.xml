<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.truckscale.common.dao.InitialInfoDao">	
	<!-- 数据字典取得 -->
	<select id="getDataDictionary" parameterType="java.util.Map" resultType="java.util.Map">
		select
		  Id
		  , Code
		  , Content 
		from
		  datadictionary 
		where
		  Type = #{Type}
	</select>
	<!-- 司机图片上传 -->
	<update id="updateExportMeasure" parameterType="java.util.Map">
		update exportMeasure
			set
				DriverAutograph = #{driverAutograph}
			where
				Id = #{id}
	</update>
</mapper>